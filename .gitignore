# Build outputs
example-app/build/
build/
*.o
*.exe

# SBOM artifacts (generated by pipeline)
sbom/
sbom-*.json

# Signing keys (never commit)
sbom-signing/
*.pem
*.sig

# Reports (CI generates; optional to ignore if you want local reports)
# reports/
